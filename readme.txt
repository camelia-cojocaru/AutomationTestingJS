Run: npx wdio run wdio.conf.js (this creates the allure-results folder)
Create report:  allure generate allure-results --clean -o allure-report (created the allure-report folder)
Open report: allure open (starts the web server of the report)